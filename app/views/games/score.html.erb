<div class='container mt-5'>
<h1>Result</h1>

<% if @word_included %>
  <%if @valid_word %>
    <p>Congratulations! <strong><%=@word%></strong> is a valid english word!</p>
  <% else %>
    <p>Sorry, but <strong><%=@word%></strong>does not seem to be a valid english word...</p>
  <% end %>
<% else %>
    <p>Sorry,but <strong><%=@word%></strong> can`t be build out of <%= @letters%>
<%end%>
<%= link_to "Play it again!", new_path %>
</div>
